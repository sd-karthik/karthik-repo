#include<stdio.h>

#if 0
int main()
{
	int num1; // it stores the number 1 value
	int num2; // it stores the number 2 value

	printf("enter the number 1:\n"); 
	scanf("%d",&num1);

	printf("enter thr number 2:\n");
	scanf("%d",&num2);

	(num1 > num2) ? printf("the num1 is max %d \n",num1) : printf("the num2 is max %d\n",num2);

}
#endif
