# include <stdio.h>
# include <unistd.h>
# include <libgen.h>
# include <malloc.h>

int main(int argc , char *argv[])
{

		printf("Dir name ==> %s\n", dirname(argv[0]));
		printf("Base name ==> %s\n", basename(argv[0]));
		return 0;
}
